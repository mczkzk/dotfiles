# Path to your Oh My Zsh installation.
export ZSH="$HOME/.oh-my-zsh"

# Set name of the theme to load --- if set to "random", it will
# load a random theme each time Oh My Zsh is loaded, in which case,
# to know which specific one was loaded, run: echo $RANDOM_THEME
# See https://github.com/ohmyzsh/ohmyzsh/wiki/Themes
ZSH_THEME="robbyrussell"

# Which plugins would you like to load?
# Standard plugins can be found in $ZSH/plugins/
# Custom plugins may be added to $ZSH_CUSTOM/plugins/
# Example format: plugins=(rails git textmate ruby lighthouse)
# Add wisely, as too many plugins slow down shell startup.
plugins=(git z fzf zsh-autosuggestions)

source $ZSH/oh-my-zsh.sh

# User configuration

# Set personal aliases, overriding those provided by Oh My Zsh libs,
# plugins, and themes. Aliases can be placed here, though Oh My Zsh
# users are encouraged to define aliases within a top-level file in
# the $ZSH_CUSTOM folder, with .zsh extension. Examples:
# - $ZSH_CUSTOM/aliases.zsh
# - $ZSH_CUSTOM/macos.zsh
# For a full list of active aliases, run `alias`.

# zsh option
setopt no_beep
setopt auto_pushd # 移動したディレクトリをスタックに自動追加
setopt pushd_ignore_dups # 重複したディレクトリはスタックに追加しない
setopt auto_cd # ディレクトリと認識できる文字列ならcdが不要
setopt hist_ignore_dups # 直前と同じコマンドを繰り返しても履歴に追加しない
setopt share_history # 他のタブやウィンドウと履歴を共有
setopt inc_append_history

# alias
alias -g C='| pbcopy' # 絞り込んでクリップボードにコピー e.g., ps aux | grep python C
# alias rm='rm -iv' # 削除時に確認をはさむ
# alias cp='cp -iv'
# alias mv='mv -iv'
alias cat='bat'

# chezmoi 用補完を有効化
eval "$(chezmoi completion zsh)"

alias cvi='chezmoi edit'
alias cpush='chezmoi apply && cd "$(chezmoi source-path)" && git add . && git commit -m "update dotfiles" && git push'
alias cdiff='chezmoi diff'
alias capply='chezmoi apply'
alias csrc='chezmoi cd'

# directory hash
# よく使うディレクトリを設定すると~c だけで移動できる
hash -d n=$HOME/NoteBackups
hash -d o=$HOME/NoteBackups/obsidian-vault0
hash -d z=$HOME/NoteBackups/zenn
hash -d g=$HOME/NoteBackups/github
hash -d c=$HOME/.claude

# これがないとVSCodeのターミナルでnode, npmなどを認識してくれない
export PATH="$PATH:/opt/homebrew/bin/"

# nodenv
eval "$(nodenv init - zsh)"

# python
export PYENV_ROOT="$HOME/.pyenv"
export PATH="$PYENV_ROOT/bin:$PATH"
eval "$(pyenv init -)"


